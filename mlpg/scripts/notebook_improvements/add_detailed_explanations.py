#!/usr/bin/env python3
"""
全てのv2ノートブックに詳細な日本語の説明と具体例を追加するスクリプト
"""

import json
import os
from pathlib import Path

def create_markdown_cell(content):
    """マークダウンセルを作成"""
    return {
        "cell_type": "markdown",
        "metadata": {},
        "source": content.split('\n')
    }

def create_code_cell(content):
    """コードセルを作成"""
    return {
        "cell_type": "code",
        "execution_count": None,
        "metadata": {},
        "outputs": [],
        "source": content.split('\n')
    }

# 各ノートブック用の詳細な説明コンテンツ
NOTEBOOK_ENHANCEMENTS = {
    "00": {
        "intro": """## 📖 このノートブックで学ぶこと

このノートブックは、機械学習の世界への入り口となる「クイックスタート」ガイドです。
料理のレシピを初めて見る人が、まず簡単な料理を作ってみるように、
機械学習も実際に動かしてみることで理解が深まります。

### なぜクイックスタートが重要なのか？

機械学習は一見複雑に見えますが、基本的な流れは次のようにシンプルです：
1. **データを用意する** - 過去の事例を集める
2. **モデルを選ぶ** - 問題に適した道具を選ぶ
3. **学習させる** - データからパターンを見つける
4. **予測する** - 新しいデータで結果を予測する

このノートブックでは、この基本的な流れを実際に体験することで、
機械学習がどのように動くのかを直感的に理解できます。

### 実世界での応用例

このクイックスタートで学ぶ技術は、以下のような実務でも使われています：
- **医療診断**: 症状から病気を予測
- **スパムフィルター**: メールが迷惑メールかどうかを判定
- **製品推薦**: 顧客の好みを予測して商品を提案
- **与信審査**: ローンの返済可能性を予測

それでは、実際にコードを動かしながら学んでいきましょう！
""",
        "data_explanation": """### 💡 データの準備について詳しく理解する

機械学習の最初のステップは「データの準備」です。これは料理で言えば材料を揃えることに相当します。

#### なぜscikit-learnのサンプルデータを使うのか？

初めて機械学習を学ぶとき、実世界のデータ収集は大変です。そこで、scikit-learnには
学習用の高品質なサンプルデータセットが用意されています。

**アイリスデータセット**は、機械学習の「Hello World」のような存在です：
- 1936年に統計学者フィッシャーが収集した歴史的なデータセット
- 3種類のアイリス（花）の測定データ
- 各花について4つの特徴（がく片・花弁の長さと幅）を記録
- 合計150個のサンプル（各種50個ずつ）

#### データの構造を理解する

機械学習のデータは通常、以下のように構成されます：

```
        特徴1      特徴2      特徴3      特徴4      ラベル
サンプル1  5.1       3.5        1.4        0.2    → アヤメ種A
サンプル2  4.9       3.0        1.4        0.2    → アヤメ種A
サンプル3  7.0       3.2        4.7        1.4    → アヤメ種B
...
```

これを「特徴行列（X）」と「ラベルベクトル（y）」に分けて扱います。
""",
        "model_explanation": """### 🤖 モデルの選択について詳しく理解する

機械学習のモデルは、データからパターンを見つけるための「アルゴリズム」です。
道具箱から適切な工具を選ぶように、問題に応じて最適なモデルを選びます。

#### ランダムフォレストとは？

**ランダムフォレスト**は、初心者にもおすすめの強力なモデルです：

**たとえ話**：
一人の専門家に意見を聞くよりも、複数の専門家の意見を集約した方が
正確な判断ができることがあります。ランダムフォレストも同じ考え方です。

**仕組み**：
1. たくさんの「決定木」（簡単な分類器）を作る
2. 各決定木はデータの異なる部分を見る
3. 全ての木の予測を多数決で決める

**利点**：
- 精度が高い（多くの問題で良い結果を出す）
- 過学習しにくい（新しいデータでも性能が安定）
- パラメータ調整が比較的簡単
- データの前処理があまり必要ない

**欠点**：
- 学習に時間がかかることがある
- モデルが大きくなりがち
- 解釈が難しい（ブラックボックス）

#### n_estimators=100 の意味

`n_estimators=100` は「100本の決定木を作る」という意味です：
- 少なすぎる（例：10本） → 精度が低い
- 多すぎる（例：1000本） → 学習に時間がかかる
- 100本 → バランスが良い（デフォルト値）
""",
        "training_explanation": """### 📚 モデルの学習について詳しく理解する

「学習」とは、モデルがデータからパターンを見つけるプロセスです。

#### 学習のプロセス

1. **パターンの探索**：
   - モデルは訓練データを分析して、特徴とラベルの関係を探します
   - 「がく片が長くて花弁が短い → 種類A」のようなルールを見つけます

2. **内部パラメータの調整**：
   - モデルは見つけたパターンを内部パラメータとして記憶します
   - ランダムフォレストの場合、各決定木の分岐ルールが保存されます

3. **検証**：
   - 訓練データで正しく分類できるか確認します
   - エラーが多い場合は、パラメータを調整します

#### `.fit()` メソッドの重要性

```python
model.fit(X_train, y_train)
```

この一行で以下のことが行われます：
- データの統計的な特性を分析
- 最適な分岐点を探索
- 100本の決定木をそれぞれ構築
- 各木のパラメータを最適化

**実行時間**：
- 小規模データ（数百〜数千行）：数秒
- 中規模データ（数万行）：数十秒〜数分
- 大規模データ（数十万行以上）：数分〜数時間

#### 料理のたとえ

学習は、料理人が何度も試作を重ねてレシピを完成させる過程に似ています：
- データ = 過去の試作記録
- 学習 = 記録から最適な調理法を見つける
- モデル = 完成したレシピ
""",
        "prediction_explanation": """### 🎯 予測について詳しく理解する

学習したモデルを使って、新しいデータの結果を予測します。

#### 予測のプロセス

1. **新しいデータの入力**：
   - まだ見たことのないデータをモデルに渡します
   - データは訓練時と同じ形式である必要があります

2. **モデルの適用**：
   - モデルは学習したパターンを新しいデータに当てはめます
   - 各決定木が個別に予測を行います

3. **結果の集約**：
   - 100本の木の予測を多数決で決定します
   - 最も多くの木が予測したクラスが最終的な予測になります

#### 予測の信頼性

予測には必ず不確実性があります：
- **高い信頼度**：全ての木が同じクラスを予測
- **低い信頼度**：木の予測が分かれる

実務では、予測の確率も重要です：
```python
# 予測確率を取得
probabilities = model.predict_proba(X_test)
# 例：[0.95, 0.03, 0.02] → 95%の確率でクラス0
```

#### 実世界での応用

**医療診断の例**：
- 入力：患者の症状データ
- 予測：病気の種類
- 活用：医師の診断を補助（最終判断は医師が行う）

**重要な注意点**：
機械学習の予測は100%正確ではありません。常に人間の判断と組み合わせて使うことが重要です。
""",
        "evaluation_explanation": """### 📊 評価について詳しく理解する

モデルの性能を測定することは、機械学習で最も重要なステップの一つです。

#### なぜ評価が必要なのか？

「学習がうまくいった」だけでは不十分です。以下を確認する必要があります：
- 新しいデータでも正しく予測できるか？
- どのくらいの精度が出ているか？
- 実用に耐えるレベルか？

#### 正解率（Accuracy）とは

正解率は最もシンプルな評価指標です：

```
正解率 = 正しく予測できた数 / 全体の数
```

**例**：
- テストデータ：100個
- 正しく予測：95個
- 間違い：5個
- 正解率 = 95/100 = 0.95 = 95%

#### 正解率の解釈

**一般的な基準**：
- 90%以上：非常に良い
- 80-90%：良い
- 70-80%：まあまあ（改善の余地あり）
- 70%未満：要改善

**ただし、問題による**：
- 簡単な問題：99%でも普通
- 難しい問題：80%でも優秀

#### 正解率だけでは不十分な場合

**例：がん診断**
- 全体の1%ががん患者の場合
- 「全員健康」と予測しても正解率99%
- しかし、がん患者を一人も見つけられない！

このような場合は、他の指標（適合率、再現率など）も見る必要があります。

#### 訓練データとテストデータの分割

機械学習では、データを2つに分けます：
- **訓練データ（70-80%）**：モデルの学習に使う
- **テストデータ（20-30%）**：性能評価に使う

**なぜ分けるのか？**：
学校のテストと同じです。問題を見ながら答えを覚えたら、本当の理解力は測れません。
""",
    },
    "01": {
        "intro": """## 📖 このノートブックで学ぶこと

データは機械学習の「燃料」です。良質なデータなしに、良いモデルは作れません。

このノートブックでは、データを人工的に生成（シミュレーション）する方法を学びます。
「なぜわざわざデータを作るの？」と思うかもしれませんが、実はこれは非常に重要なスキルです。

### データシミュレーションの重要性

**1. 学習段階での利用**：
- 実際のデータ収集は時間とコストがかかる
- シミュレーションなら即座にデータを用意できる
- 様々なパターンのデータで実験できる

**2. モデルの検証**：
- 正解が分かっているデータでテストできる
- モデルの限界を確認できる
- バグの発見に役立つ

**3. プロトタイプ開発**：
- 実データが無くても開発を進められる
- データ構造の設計を先行できる
- デモやプレゼンテーションに使える

### 実世界での応用

シミュレーションデータは、以下の分野で実際に使われています：
- **製薬研究**：薬の効果をシミュレーション
- **金融工学**：市場の動きをシミュレーション
- **自動運転**：様々な交通状況をシミュレーション
- **気象予測**：気候変動のシミュレーション

それでは、scikit-learnを使って様々なデータを生成してみましょう！
""",
        "classification_data": """### 🎯 分類用データの生成

分類問題とは、データを複数のカテゴリーに分ける問題です。

#### 具体例で理解する

**スパムメール判定**：
- 入力：メールの特徴（単語の出現頻度など）
- 出力：スパム or 正常

**顧客セグメンテーション**：
- 入力：購買履歴、年齢、収入など
- 出力：セグメントA、B、C

#### make_classification() の仕組み

`make_classification()` は、指定した条件で分類データを生成します：

```python
X, y = make_classification(
    n_samples=100,      # サンプル数
    n_features=20,      # 特徴量の数
    n_informative=2,    # 有用な特徴量の数
    n_redundant=10,     # 冗長な特徴量の数
    random_state=42     # 再現性のためのシード
)
```

**各パラメータの意味**：

**n_samples（サンプル数）**：
- 生成するデータポイントの数
- 多いほど学習が安定するが、計算時間も増える
- 推奨：最低でも特徴量数の10倍以上

**n_features（特徴量数）**：
- データの次元数
- 多すぎると「次元の呪い」が発生
- 実務では10〜1000程度が多い

**n_informative（有用な特徴量数）**：
- 実際にクラス分けに役立つ特徴量の数
- 少なすぎると学習が難しい
- 多すぎると現実離れする

**n_redundant（冗長な特徴量数）**：
- 他の特徴量の線形結合で表せる特徴量
- 現実のデータには冗長性がよくある
- モデルの頑健性をテストできる

#### データの可視化の重要性

データを見ることで、以下が分かります：
- クラスが明確に分かれているか
- 外れ値があるか
- データの分布はどうか

「百聞は一見にしかず」です。必ず可視化しましょう！
""",
        "regression_data": """### 📈 回帰用データの生成

回帰問題とは、連続的な数値を予測する問題です。

#### 分類と回帰の違い

| 分類 | 回帰 |
|------|------|
| カテゴリーを予測 | 数値を予測 |
| 例：犬 or 猫 | 例：価格、気温 |
| 離散的 | 連続的 |

#### 実世界の回帰問題

**住宅価格予測**：
- 入力：広さ、築年数、立地など
- 出力：価格（100万円、200万円...）

**売上予測**：
- 入力：広告費、季節、過去の売上など
- 出力：来月の売上額

**気温予測**：
- 入力：湿度、風速、気圧など
- 出力：明日の最高気温

#### make_regression() のパラメータ

```python
X, y = make_regression(
    n_samples=100,      # サンプル数
    n_features=1,       # 特徴量数
    noise=10,           # ノイズの大きさ
    random_state=42
)
```

**noise（ノイズ）の重要性**：
- 現実のデータには必ずノイズ（誤差）がある
- 測定誤差、記録ミス、予測不可能な要因など
- ノイズが多いほど予測は難しくなる

**ノイズの例**：
- 住宅価格：同じ条件でも個別の事情で価格が変動
- 気温予測：局所的な気象現象による誤差
- 売上予測：突発的なイベントによる変動

#### 線形と非線形の関係

**線形関係**：
```
y = 2x + 3
```
グラフにすると直線になる

**非線形関係**：
```
y = x² + 2x + 3
```
グラフにすると曲線になる

実世界の多くの問題は非線形です。
""",
    },
    "02": {
        "intro": """## 📖 このノートブックで学ぶこと

「ゴミを入れればゴミが出る（Garbage In, Garbage Out）」という言葉があります。
どんなに優れたモデルでも、データが悪ければ良い結果は出ません。

このノートブックでは、生のデータを機械学習モデルが扱いやすい形に変換する
**前処理**と**特徴量エンジニアリング**を学びます。

### なぜ前処理が必要なのか？

実世界のデータは、以下のような問題を抱えていることが多いです：
- 欠損値（データが抜けている）
- スケールの違い（年齢は0-100、年収は0-1億円）
- カテゴリカルデータ（色、性別など）
- 外れ値（異常に大きい/小さい値）

これらをそのまま使うと、モデルの性能が大幅に下がります。

### 料理のたとえ

前処理は、料理の「下ごしらえ」に似ています：
- 野菜を洗う → 欠損値の処理
- 材料を切り揃える → スケーリング
- 調味料を準備する → 特徴量エンジニアリング

下ごしらえをしっかりすれば、美味しい料理（高精度なモデル）ができます！

### 実務での重要性

データサイエンティストの仕事の**60-80%は前処理**だと言われています。
モデル構築よりも、データの準備の方が時間がかかるのです。

それでは、実際に様々な前処理技術を学んでいきましょう！
""",
        "missing_values": """### 🔍 欠損値の扱い

欠損値とは、データが存在しない（記録されていない）状態のことです。

#### 欠損値が発生する理由

**1. 測定エラー**：
- センサーの故障
- 通信エラー
- 人的ミス

**2. 意図的な欠損**：
- アンケートで回答したくない項目
- プライバシー保護のため
- 該当しない項目（未婚者の配偶者情報など）

**3. データ統合時の問題**：
- 異なるデータソースの結合時
- フォーマットの不一致
- タイムスタンプのずれ

#### 欠損値の処理方法

**1. 削除**：
```python
# 欠損値を含む行を削除
df.dropna()
```
- 利点：シンプル
- 欠点：データが減る
- 推奨：欠損が少ない場合（5%未満）

**2. 平均値で補完**：
```python
# 平均値で欠損値を埋める
from sklearn.impute import SimpleImputer
imputer = SimpleImputer(strategy='mean')
```
- 利点：データ量を保てる
- 欠点：データの分散が小さくなる
- 推奨：数値データで、欠損がランダムな場合

**3. 中央値で補完**：
```python
imputer = SimpleImputer(strategy='median')
```
- 利点：外れ値の影響を受けにくい
- 推奨：外れ値が多いデータ

**4. 最頻値で補完**：
```python
imputer = SimpleImputer(strategy='most_frequent')
```
- 利点：カテゴリカルデータにも使える
- 推奨：カテゴリカルデータ

#### 補完の実例

**年齢データの欠損**：
```
[25, 30, 欠損, 28, 35]
↓ 平均値(29.5)で補完
[25, 30, 29.5, 28, 35]
```

**重要な注意点**：
欠損値が「ランダムではない」場合は注意が必要です。
例えば、高所得者ほど年収を答えない傾向がある場合、
単純に平均値で補完すると偏りが生じます。
""",
        "scaling": """### 📏 スケーリング（正規化）

スケーリングとは、異なるスケールの特徴量を同じ範囲に揃えることです。

#### なぜスケーリングが必要なのか？

**例：住宅価格予測**
- 特徴1：部屋数（1-10）
- 特徴2：面積（20-200㎡）
- 特徴3：築年数（0-50年）
- 特徴4：価格（1000万-1億円）

これらをそのまま使うと、価格の影響が強くなりすぎます。

#### スケーリングしないとどうなるか

多くの機械学習アルゴリズムは、特徴量の大きさに影響を受けます：

**距離ベースのアルゴリズム**（k-NN、SVMなど）：
```
サンプル1: [2部屋, 50㎡, 10年]
サンプル2: [3部屋, 55㎡, 12年]

距離 = √((3-2)² + (55-50)² + (12-10)²)
     = √(1 + 25 + 4) = √30 ≈ 5.5

面積の影響が最も大きい！
```

**勾配降下法を使うアルゴリズム**（ニューラルネットワークなど）：
- スケールが大きい特徴量の方向に学習が偏る
- 収束が遅くなる
- 局所解に陥りやすい

#### StandardScaler（標準化）

平均0、標準偏差1に変換します：

```python
z = (x - μ) / σ
```

**例**：
```
元データ: [10, 20, 30, 40, 50]
平均: 30
標準偏差: 約15.8

変換後: [-1.26, -0.63, 0, 0.63, 1.26]
```

**利点**：
- 外れ値の影響を比較的受けにくい
- 正規分布を仮定するモデルに適している

**欠点**：
- 外れ値があると効果が薄れる
- 値の範囲が固定されない（-3〜3程度になることが多い）

#### MinMaxScaler（最小最大正規化）

0から1の範囲に変換します：

```python
x_scaled = (x - x_min) / (x_max - x_min)
```

**例**：
```
元データ: [10, 20, 30, 40, 50]
最小値: 10
最大値: 50

変換後: [0, 0.25, 0.5, 0.75, 1.0]
```

**利点**：
- 値の範囲が明確（0〜1）
- 解釈しやすい

**欠点**：
- 外れ値の影響を強く受ける
- 新しいデータで範囲外の値が出る可能性

#### どちらを選ぶべきか？

**StandardScalerを推奨**：
- 線形回帰、ロジスティック回帰
- SVM
- ニューラルネットワーク

**MinMaxScalerを推奨**：
- ニューラルネットワーク（活性化関数がsigmoidの場合）
- 画像処理（ピクセル値を0-1に）

**スケーリング不要**：
- 決定木
- ランダムフォレスト
- 勾配ブースティング

これらの木ベースのアルゴリズムは、分岐点を探すだけなので、
スケールの影響を受けません。
""",
        "encoding": """### 🏷️ カテゴリカルデータのエンコーディング

カテゴリカルデータとは、数値ではなくラベル（文字列）で表されるデータです。

#### カテゴリカルデータの例

**名義尺度**（順序に意味がない）：
- 色：赤、青、緑
- 性別：男性、女性、その他
- 都道府県：東京、大阪、北海道...

**順序尺度**（順序に意味がある）：
- サイズ：S、M、L、XL
- 評価：悪い、普通、良い
- 学歴：高卒、大卒、院卒

#### なぜエンコーディングが必要なのか？

機械学習アルゴリズムは、基本的に数値しか扱えません。
「赤」という文字列をそのまま計算に使うことはできないのです。

#### One-Hot Encoding（ワンホットエンコーディング）

各カテゴリーを0/1のバイナリベクトルに変換します。

**例：色のエンコーディング**
```
元データ: [赤, 青, 緑, 赤]

変換後:
        赤  青  緑
サンプル1  1   0   0
サンプル2  0   1   0
サンプル3  0   0   1
サンプル4  1   0   0
```

**仕組み**：
- カテゴリーごとに新しい列を作る
- 該当するカテゴリーだけ1、他は0

**利点**：
- 順序関係を仮定しない
- どのモデルでも使える
- 解釈しやすい

**欠点**：
- カテゴリーが多いと列が爆発的に増える
- 次元の呪いが発生しやすい

**推奨される使用ケース**：
- カテゴリー数が少ない（10個以下）
- 順序に意味がない
- 線形モデル、ニューラルネットワーク

#### Label Encoding（ラベルエンコーディング）

各カテゴリーを整数に変換します。

**例**：
```
元データ: [赤, 青, 緑, 赤]
変換後:   [0, 1, 2, 0]
```

**利点**：
- 列数が増えない
- シンプル

**欠点**：
- 順序関係を仮定してしまう
- 線形モデルには不適切

**推奨される使用ケース**：
- 決定木、ランダムフォレストなど木ベースのモデル
- 順序尺度のデータ
- カテゴリー数が非常に多い場合

#### 実世界での応用例

**Eコマースサイトの商品分類**：
```
カテゴリー: 家電、衣類、食品、本...

One-Hot Encoding:
家電 衣類 食品 本
1    0    0   0  → 家電
0    1    0   0  → 衣類
```

**顧客セグメンテーション**：
```
地域: 北海道、東北、関東、中部、近畿、中国、四国、九州

Label Encoding:
北海道→0, 東北→1, 関東→2, ...

注意: 地域間に順序関係はないので、
     木ベースのモデル以外ではOne-Hotを使うべき
```

#### ダミー変数トラップ

One-Hot Encodingで注意すべき点：

```
例: 性別（男性、女性）

通常:
男性 女性
1    0
0    1

問題点: 男性列と女性列は完全に相関（片方が分かればもう片方も分かる）

解決策: drop='first' を使う
女性
0   → 男性
1   → 女性
```

これを「ダミー変数トラップ」と言います。
線形回帰など一部のモデルでは問題になるため、
1列削除することが推奨されます。
""",
    },
}

def add_detailed_explanations(nb_path):
    """ノートブックに詳細な説明を追加"""
    print(f"Processing: {nb_path}")

    # ノートブックを読み込む
    with open(nb_path, 'r', encoding='utf-8') as f:
        nb = json.load(f)

    # ノートブック番号を取得
    nb_num = Path(nb_path).name.split('_')[0]

    # 該当する説明があれば追加
    if nb_num in NOTEBOOK_ENHANCEMENTS:
        enhancements = NOTEBOOK_ENHANCEMENTS[nb_num]
        new_cells = []

        for i, cell in enumerate(nb['cells']):
            # セルを追加
            new_cells.append(cell)

            # イントロダクションを最初に追加
            if i == 0 and 'intro' in enhancements:
                new_cells.insert(1, create_markdown_cell(enhancements['intro']))

            # コードセルの前に説明を追加
            if cell['cell_type'] == 'code' and cell.get('source'):
                source_text = ''.join(cell['source']).lower()

                # データ関連の説明
                if 'data' in enhancements and ('load' in source_text or 'dataset' in source_text or 'make_' in source_text):
                    if i > 0:  # 最初のセルでない場合
                        new_cells.insert(-1, create_markdown_cell(enhancements.get('data_explanation', '')))

                # 分類データの説明
                if 'classification_data' in enhancements and 'make_classification' in source_text:
                    new_cells.insert(-1, create_markdown_cell(enhancements['classification_data']))

                # 回帰データの説明
                if 'regression_data' in enhancements and 'make_regression' in source_text:
                    new_cells.insert(-1, create_markdown_cell(enhancements['regression_data']))

                # 欠損値の説明
                if 'missing_values' in enhancements and ('impute' in source_text or 'fillna' in source_text or 'dropna' in source_text):
                    new_cells.insert(-1, create_markdown_cell(enhancements['missing_values']))

                # スケーリングの説明
                if 'scaling' in enhancements and ('scaler' in source_text or 'standard' in source_text or 'minmax' in source_text):
                    new_cells.insert(-1, create_markdown_cell(enhancements['scaling']))

                # エンコーディングの説明
                if 'encoding' in enhancements and ('encode' in source_text or 'get_dummies' in source_text or 'labelencoder' in source_text):
                    new_cells.insert(-1, create_markdown_cell(enhancements['encoding']))

                # モデル関連の説明
                if 'model_explanation' in enhancements and ('randomforest' in source_text or 'classifier' in source_text):
                    new_cells.insert(-1, create_markdown_cell(enhancements['model_explanation']))

                # 学習の説明
                if 'training_explanation' in enhancements and '.fit(' in source_text:
                    new_cells.insert(-1, create_markdown_cell(enhancements['training_explanation']))

                # 予測の説明
                if 'prediction_explanation' in enhancements and '.predict(' in source_text:
                    new_cells.insert(-1, create_markdown_cell(enhancements['prediction_explanation']))

                # 評価の説明
                if 'evaluation_explanation' in enhancements and ('score' in source_text or 'accuracy' in source_text):
                    new_cells.insert(-1, create_markdown_cell(enhancements['evaluation_explanation']))

        nb['cells'] = new_cells

    # 保存
    with open(nb_path, 'w', encoding='utf-8') as f:
        json.dump(nb, f, ensure_ascii=False, indent=1)

    print(f"✅ Enhanced: {nb_path}")

def main():
    """メイン処理"""
    print("=" * 60)
    print("📚 全ノートブックに詳細な説明を追加")
    print("=" * 60)
    print()

    notebooks_dir = Path("/home/user/machine-learning-playground/notebooks")

    # 全てのv2ノートブックを処理
    notebooks = sorted(notebooks_dir.glob("*_improved_v2.ipynb"))

    for nb_path in notebooks:
        add_detailed_explanations(nb_path)
        print()

    print("=" * 60)
    print(f"✅ 完了！{len(notebooks)}個のノートブックを拡張しました")
    print("=" * 60)

if __name__ == "__main__":
    main()
